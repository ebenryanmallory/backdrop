{
  "name": "backdrop",
  "version": "1.0.0",
  "main": "web/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "flydeploy": "fly deploy --build-secret SHOPIFY_API_KEY=<SHOPIFY_API_KEY> --remote-only"
  },
  "dependencies": {
    "@shopify/app": "3.44.1",
    "@shopify/cli": "3.44.1"
  },
  "author": "Eben Mallory - Motion Storyline"
}